import { Link } from "react-router-dom";

export default function NavTitle() {
  return (
    <Link to="/">
      <div className="flex items-center gap-1 font-bold">MFA SAMPLE</div>
    </Link>
  );
}
